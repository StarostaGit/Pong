#ifndef PARAMETERS_H
#define PARAMETERS_H

const float WIDTH = 1200;
const float HEIGHT = 800;
const float X_OFFSET = 50; // x coordinate of the left corner of the white frame
const float Y_OFFSET = 50; // y coordinate of the left corner of the white frame

const float BALL_RADIUS = 10;
const float PADDLE_WIDTH = 15;
const float PADDLE_HEIGHT = 100;

const float PADDLE_SPEED = 5;
const float SPACE_BEFORE_PADDLES = 70;
const int BALL_SPEED_RANGE = 5;
const int BALL_MIN_SPEED = 12;


#endif // PARAMETERS_H
